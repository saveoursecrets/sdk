[package]
name = "sos-node"
version = "0.1.0"
edition = "2021"
description = "Distributed, encrypted database for private secrets."
homepage = "https://saveoursecrets.com"
license = "GPL-3.0"

[dependencies]
sos-core = { path = "../core", version = "0.1.0" }

thiserror = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
async-trait = "0.1"
async-recursion = "1"
k256 = { version = "0.11", features = ["ecdsa", "sha256", "keccak256"] }
web3-signature = { version = "0.2.5", features = ["single-party"] }
tokio = { version = "1", features = ["rt", "rt-multi-thread", "time", "sync"] }
serde = "1"
serde_json = "1"
rand = "0.8"
url = "2"
futures = "0.3"
pin-project-lite = "0.2"
uuid = { version = "1", features = ["serde", "v4"] }
hex = "0.4"
tempfile = "3"
dirs = "4"
http = "0.2"
bs58 = "0.4"
secrecy = "0.8"
log = "0.4"
crypto-bigint = "0.4.8"
sha3 = "0.10"

# client
web3-keystore = { version = "0.4", optional = true }

# server
toml = { version = "0.5", optional = true }
axum = { version = "0.5", features = ["headers", "ws"], optional = true }
axum-macros = { version = "0.2", optional = true }
axum-server = { version = "0.4", features = ["tls-rustls"], optional = true }
hyper = { version = "0.14", features = ["full"], optional = true }
tower = { version = "0.4", optional = true }
tower-http = { version = "0.3", features = ["full"], optional = true }
once_cell = { version = "1.10", optional = true }
rust-embed = { version = "6", optional = true }
mime_guess = { version = "2", optional = true }
async-stream = { version = "0.3", optional = true }
tokio-stream = { version = "0.1", optional = true }

# agent
[target.'cfg(not(target_arch="wasm32"))'.dependencies]
parity-tokio-ipc = { version = "0.9", optional = true }
binary-stream = { version = "3", features = ["wasm32"], optional = true }
tokio-tungstenite = { version = "0.17.2", features = ["rustls-tls-native-roots"] }

[target.'cfg(any(not(target_arch="aarch64"),not(target_os="linux")))'.dependencies]
reqwest = { version = "0.11", default-features = true, features = ["json", "rustls-tls"], optional = true }

# NOTE: Must disable default-features so that openssl
# NOTE: is not compiled when cross-compiling in CI.
#
# NOTE: If we disable `default-features` during
# NOTE: development then attempting to connect to 
# NOTE: the changes feed using the locally issued mkcert
# NOTE: certificate yields an "Unknown Issuer" error.
# NOTE: Which is related to the `security-framework` not 
# NOTE: being compiled on macos when `default-features` is 
# NOTE: disabled.
[target.'cfg(all(target_arch="aarch64",target_os="linux"))'.dependencies]
reqwest = { version = "0.11", default-features = false, features = ["json", "rustls-tls"], optional = true }

[dev-dependencies]
anyhow = "1"

[features]
default = ["node"]
client = [
  "reqwest",
  "web3-keystore",
]
node = ["client", "server"]
server = [
  "toml",
  "axum",
  "axum-macros",
  "axum-server",
  "hyper",
  "tower",
  "tower-http",
  "once_cell",
  "rust-embed",
  "mime_guess",
  "async-stream",
  "tokio-stream",
]
gui = []
agent-client = ["parity-tokio-ipc", "binary-stream"]
agent-server = ["parity-tokio-ipc", "binary-stream"]

[package.metadata.release]
tag = false
shared-version = true
