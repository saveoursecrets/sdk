[package]
name = "sos"
version = "0.1.0"
edition = "2021"
description = "Distributed, encrypted database for private secrets."
homepage = "https://saveoursecrets.org"
license = "GPL-3.0"
default-run = "sos"
publish = false

[workspace]
members = [
  "workspace/agent",
  "workspace/audit",
  "workspace/check",
  "workspace/client",
  "workspace/core",
  "workspace/node",
  "workspace/readline",
  "workspace/server",
]

[dependencies]
sos-agent = { path = "workspace/agent", version = "0.1.0" }
sos-audit = { path = "workspace/audit", version = "0.1.0" }
sos-check = { path = "workspace/check", version = "0.1.0" }
sos-client = { path = "workspace/client", version = "0.1.0" }
sos-node = { path = "workspace/node", version = "0.1.0" }
sos-core = { path = "workspace/core", version = "0.1.0" }
sos-server = { path = "workspace/server", version = "0.1.0" }

thiserror = "1"
clap = { version = "3", features = ["derive", "wrap_help"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[dev-dependencies]
anyhow = "1"
axum-server = "0.4" 
tokio = { version = "1", features = ["full"] }
url = "2"
web3-keystore = "0.4"
serde_json = "1"
tracing = "0.1"
futures = "0.3"
serial_test = "0.8"
secrecy = "0.8"

[[bin]]
name = "sos"
path = "src/bin/main.rs"

[workspace.metadata.release]
allow-branch = ["main"]
sign-tag = true
sign-commit = true
pre-release-hook = ["cargo", "audit"]
pre-release-commit-message = "Release {{version}}"
post-release-commit-message = "Start next version {{next_version}}"
